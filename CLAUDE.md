# 공부의 온도 (firststudy.co.kr) — 프로젝트 안내

## 개요
- 초·중·고 1:1 과외 상담 유치 사이트 (정적 사이트, GitHub Pages)
- **라이브 주소:** https://firststudy.co.kr
- **GitHub 저장소:** https://github.com/x26589334-cpu/tutoring-site (기본 브랜치: **main**)
- **상담 접수:** 구글 "웹 문의" 시트의 `공부의온도` 탭 (공용 Apps Script 웹앱)
- 형제 사이트: **티칭코칭**(perfectedu.co.kr, 저장소 `gwaoe-page`) · **와와**(wawa-renewal) — 같은 회사

## 배포 방법 (수정 → 반영)
```
git add .
git commit -m "메시지"
git push
```
→ 1~2분 후 라이브 반영. 캐시로 안 보이면 Ctrl+Shift+R.

## 파일 구조
- `index.html` — 홈. 히어로 / 세 가지 방식(방문·화상·학습센터) / 상담 폼
- `teachers.html` — **선생님 찾기**. 검색 + 방식 탭 + 과목·지역·성별·유아 필터, 30명씩 "더 보기"
- `teachers-data.js` — 선생님 목록 데이터 `window.TEACHERS=[{i,n,g,c,s,gr,sd,r,tag,k}]`
  - `i`=publicId, `n`=마스킹 이름, `g`=성별, `c`=수업방식(방문/화상/방문+화상), `s`=과목[], `gr`=학년[], `sd`=시도, `r`=방문지역, `tag`=한 줄 소개, `k`=유아·아동 전문
- `teachers-search.js` — 검색 색인 `{id: '시군구 읍면동 + 학교 + 소개글'}` (defer 로드, 초기 로딩 경량화)
- `t/{publicId}.html` — 선생님 개별 페이지 **750개**
- `centers.html` — **학습센터 찾기**. 검색 + 지역 필터 + 30곳씩 "더 보기" + 지도 팝업 (와와 `centers.html` 기능을 그대로 옮김)
- `centers-data.js` — 학습센터 **205개** `window.CENTERS=[{name,region,addr,elem,mid,high,dong}]`
- `status.html` — 이번 달 수업 현황 (현재 내용은 예시 데이터, 자동 갱신 장치 없음)
- `style.css` 공용 스타일 / `sitemap.xml` / `robots.txt` / `CNAME`(firststudy.co.kr)
- `google-apps-script.gs` — 상담 폼 수신 Apps Script **참고용 사본** (실제 코드는 Apps Script 편집기에 있음)
- `c/{센터명}.html` — 센터 개별 페이지 **195개** (지역·학교명·과목 키워드 노출용)
- `blog.html` + `blog/{날짜}-{슬러그}.html` — **공부 이야기** (하루 한 편) / `rss.xml`
- `tools/` — 재생성 스크립트 (아래 참고)

## ⚠️ 중요 규칙
- **`style.css` 를 수정하면 전 페이지의 `style.css?v=` 숫자를 올려야 반영된다.** (현재 **v5**)
  안 올리면 배포는 돼도 브라우저가 옛 캐시를 써서 "바뀐 게 없다"는 현상이 난다.
  ```bash
  V=5
  grep -rl 'style.css?v=' --include='*.html' . | while read f; do
    sed -i "s#style\.css?v=[0-9]*#style.css?v=$V#g" "$f"
  done
  grep -rho 'style\.css?v=[0-9]*' --include='*.html' . | sort | uniq -c   # 전부 같은 버전이어야 함
  ```
  `t/` 750개 · `c/` 195개 · `blog/` 도 함께 바뀌므로 `-r` 로 돌릴 것. (현재 총 950곳)
- **선생님(`t/`)·센터(`c/`)·글(`blog/`) 페이지를 추가·삭제하면 `tools/build_sitemap.ps1` 을 다시 실행**해 `sitemap.xml` 을 갱신한다.
- **계열 브랜드명이 사이트에 나오면 안 된다** (와와·모두오름·글로리드·더블유플러스·에듀코·WAWA).
  공부의 온도는 독립 브랜드로 보여야 한다. 센터는 `build_centers.ps1`, 선생님은 `build_data.ps1` 의
  `Remove-Brand` 가 처리한다. **2026-09-09 에 선생님 소개글 9곳이 빠져나가 있던 걸 고쳤다** —
  `build_data.ps1` 에만 제거 로직이 없었던 게 원인. 데이터를 다시 만들면 아래로 반드시 확인할 것.
  ```bash
  grep -rl '와와\|WAWA\|모두오름\|글로리드\|더블유플러스\|에듀코' --include='*.html' --include='*.js' . 
  # CLAUDE.md 와 tools/*.ps1 (제거 규칙 자체) 말고는 아무것도 안 나와야 한다
  ```
- 커밋은 Applefist 계정으로.

## 선생님 750명 데이터
- 출처: **와와 사내 코치 데이터**를 두 저장소에서 합쳐 만든다. 원본을 직접 긁지 않는다.
  - `~/wawa-renewal/assets/js/coaches-data.js` → `tag`(한 줄 소개), `intro`(소개 전문), `schools`, `emdong`, `sigungu`
  - `~/gwaoe-page/teachers-data.js` → `i`(publicId), `n`(ㅇ 마스킹 이름), `g`(성별), `gr`(학년 범위), `k`(유아)
  - **두 파일은 인덱스 순서가 정확히 일치**한다(750 = 750, 이름 동일). 이 순서 일치를 전제로 병합한다.
- 이름은 가운데 글자 **ㅇ 마스킹**(박인옥 → 박ㅇ옥). 사진은 쓰지 않고 이니셜 사각형으로 대체.
- 소개글은 와와 쪽에서 이미 정제된 것(내부 조직 태그·외모 언급·검증 불가 성과 주장 제거)을 그대로 쓴다.

### 재생성 방법
```powershell
# PowerShell 5.1 은 BOM 없는 .ps1 을 CP949 로 읽어 한글이 깨진다.
# tools/ 안의 스크립트는 UTF-8 BOM 으로 저장돼 있으니 그대로 실행하면 된다.
.\tools\build_data.ps1      # 와와 + 티칭코칭 → teachers-data.js, teachers-search.js
.\tools\gen_teachers.ps1    # → t/*.html 750개
.\tools\build_sitemap.ps1   # → sitemap.xml
```
`build_data.ps1` 은 `~/wawa-renewal` 과 `~/gwaoe-page` 가 로컬에 clone 돼 있어야 동작한다.
중간 파일 `%TEMP%\td_detail.json`(소개 전문·학교 목록)을 `gen_teachers.ps1` 이 읽으므로 **순서대로** 실행할 것.

**PowerShell 함정**: 변수명은 대소문자를 구분하지 않는다. `$w=$W[$n]` 처럼 쓰면 원본 배열 `$W` 가 첫 원소로 덮여 루프가 1회만 돈다. 루프 변수는 `$cw`/`$ct` 처럼 다른 이름을 쓸 것.

## 학습센터 205개 (2026-09-08 추가)
- 출처: **와와 센터 데이터**(`~/wawa-renewal/assets/js/centers-data.js`)를 그대로 옮기되, **브랜드 표기를 전부 제거**한다.
  - `name`: `목감점(모두)` → `목감점` (괄호 안 브랜드 제거)
  - `addr`: `… 501호 와와학습코칭학원` → `… 501호` (와와·모두오름·글로리드·더블유플러스 제거)
  - 와와 센터 상세 페이지(`page` 필드)는 옮기지 않는다. 우리 사이트에서 갈 곳이 없다.
- 컨셉은 **"그냥 와서, 편하게 공부하는 곳"**. 브랜드가 아니라 *공부하는 자리*로 소개한다.

- 검색 로직은 와와 것을 그대로 썼다. 학교 정식명칭↔약칭 통일(`중학교`→`중`) 후 **공백으로 나눈 토큰이 전부 포함된 센터만** 남긴다(`분당 불곡중` 같은 조합 검색 지원).
- 재생성: `.\tools\build_centers.ps1` (와와 저장소가 로컬에 있어야 함)
- ⚠️ **전화번호를 넣지 않았다.** 이 사이트에는 전화번호가 어디에도 없어서, 와와의 "📞 전화" 버튼 자리를 **"상담 신청"** 으로 바꿨다. 전화 노출이 필요하면 사용자에게 번호를 확인하고 넣을 것.

### 센터 개별 페이지 `c/{센터명}.html` — **195개** (2026-09-10 신설)
`centers.html` 은 JS 로만 그려서 **학교명 1,700여 개가 검색엔진에 안 잡혔다.** 그걸 정적 HTML 로 꺼낸 페이지다.
현재 195개 페이지에 **학교명 5,394회 + 지역·과목 키워드**가 정적으로 깔려 있다.

- 제목 형식: `{시군구} {동} 수학·영어 과외 — {중}·{중}·{고} 내신 | 공부의 온도`
- 본문: 인근 초·중·고 전체 나열 / 과목 5개 문단(`{지역} {과목}과외` h3) / 방문·화상·센터 3방식 / 그 지역 선생님 3명 / 주변 센터 링크 / 상담 CTA
- **205곳인데 페이지는 195개**다. 같은 이름의 지점이 여러 곳 있어(수지점 3, 은평점 3 등 8그룹) 따로 만들면 거의 같은 페이지가 생긴다. **하나로 합치고 학교 목록은 합집합, 주소는 전부 싣는다.**
- ⚠️ **파일명은 한글 그대로 저장한다**(`c/가경점.html`). 퍼센트 인코딩된 문자열을 파일명으로 쓰면 브라우저가 URL 을 디코딩해서 요청하기 때문에 **전부 404 가 된다.** 대신 `sitemap.xml` 과 `canonical` 은 퍼센트 인코딩 형태로 쓴다.
- `centers.html` 카드의 센터 이름과 "자세히 보기"가 이 페이지로 연결된다.
- 재생성: `.\tools\gen_centers.ps1` → 그다음 반드시 `.\tools\build_sitemap.ps1`

## 메뉴 구조 (2뎁스)
```
과외 ▾ ── 방문과외 (index.html#visit)
       └─ 화상과외 (index.html#online)
학습센터   (centers.html)
선생님 찾기 (teachers.html)
공부 이야기 (blog.html)
수업 현황   (status.html)
```
- 데스크톱은 hover 드롭다운, 모바일(600px 이하)은 하위 항목을 들여쓰기해 **항상 펼쳐서** 보여준다.
- 메뉴를 바꾸면 `index/status/teachers/centers/blog` 5개 + **`t/` 750개 + `c/` 195개**를 전부 고쳐야 한다.
  생성 페이지는 `tools/gen_teachers.ps1` · `tools/gen_centers.ps1` 안의 nav 블록을 고치고 다시 돌리면 된다.

## 티칭코칭과 일부러 다르게 한 점
같은 회사 사이트라 데이터는 공유하지만, **화면이 겹치면 두 사이트가 서로 잡아먹는다.** 아래는 의도적 차별화이므로 되돌리지 말 것.

| | 티칭코칭 (perfectedu) | 공부의 온도 (firststudy) |
|---|---|---|
| 1차 축 | 과목·지역 | **수업 방식**(방문/화상) — 브랜드 3색으로 구분 |
| 카드 | 이니셜 원 + 과목 칩 + 지역 | **한 줄 소개(tag)를 헤드라인**으로 세움 |
| 목록 | 24명씩 페이지네이션 | 30명씩 **"더 보기"** |
| 상세 URL | `teacher-{id}.html` (루트) | `t/{id}.html` (폴더 분리) |
| 상세 하단 | 학교 링크 중심 | **"이런 선생님도 있어요"** 3명 추천 |
| 톤 | 정보형 | 크림색·고운돋움, 학부모 대화체 |

브랜드 색 토큰: 방문=`--visit`(애프리콧) / 화상=`--online`(스카이) / 학습센터=`--center`(민트).
선생님 카드 왼쪽 색 띠와 배지가 이 토큰을 따른다.

## 선생님 → 상담 연결
선생님 상세의 CTA 는 `index.html?t={publicId}&n={이름}#contact`, 센터 카드의 CTA 는 `index.html?c={센터명}#contact` 로 이동한다.
홈에서 이 파라미터를 읽어 폼 위에 "OOO 선생님을 보고 오셨네요" 안내를 띄우고,
접수 데이터에 `희망선생님` / `희망센터` 항목을 함께 보낸다.
공용 Apps Script 는 **새 항목이 오면 시트 헤더에 자동으로 컬럼을 추가**하므로 서버 쪽 수정은 필요 없다.

## 데일리 운영 — 하루 한 편 (2026-09-10 확정)

콘텐츠 축은 **지역 + 학교명 + 과목과외**다. 하루 **한 편**이면 충분하다 — 195개 동네 × 5과목 = **975편**, 2년 반치다.

### 하루 순서
```powershell
.\tools\next_post.ps1        # 오늘 쓸 동네·과목 제안 (안 쓴 조합만, 학교 많은 곳부터)
#  → blog/{YYYY-MM-DD}-{영문슬러그}.html 을 손으로 쓴다
.\tools\build_blog.ps1       # blog.html 목록 + rss.xml 갱신
.\tools\build_sitemap.ps1    # sitemap.xml 갱신
git add . ; git commit -m "..." ; git push
```

### 글에 반드시 넣을 meta 4개
`build_blog.ps1` 과 `next_post.ps1` 이 이걸 읽는다. 빠지면 목록·RSS에 안 잡히고 중복 조합도 못 거른다.
```html
<meta name="article:published" content="2026-09-10">
<meta name="article:area"      content="용인시 풍덕천동">
<meta name="article:subject"   content="수학">
<meta name="article:center"    content="수지점">
```

### 제목 형식
```
{시군구} {동} {학년}{과목}과외 — {증상 한 문장}
예) 수지 풍덕천동 중2 수학과외 — 계산은 맞는데 서술형에서 깎인다면
```
**제목은 나중에 고치지 않는다.** 제목을 바꾸면 순위가 초기화된다. 티칭코칭이 노출 중인 글을 반복 수정했다가 44→12 로 떨어진 전례가 있다. **새 글만 추가한다.**

### 키워드 배치 (네이버 기준)
네이버는 반복을 감점한다. 문장에 억지로 끼워 넣지 말고, **원래 쓸 문장을 검색어와 같은 단어로 바꿔 쓴다.**

| 키워드 | 본문 허용 | 넣는 자리 |
|---|---|---|
| `{학년}{과목}과외` | **2~3회** | 첫 문단 1회 · 마무리 박스 1회 |
| `{동}`, `{시군구}` | **3~4회** | 첫 문단 · 학교 문단 · 하단 링크 |
| 학교명 | 각 **1회** | "이 동네 아이들은 A중, B중을 거쳐 C고, D고로 갑니다" 식으로 한 문단에 몰아서 |
| 주제어(서술형·내신 등) | 제한 없음 | 글의 핵심이라 자연스럽게 많이 나온다 |

기준 글: [`blog/2026-09-10-suji-pungdeokcheon-math.html`](blog/2026-09-10-suji-pungdeokcheon-math.html) — 본문 1,865자에 `중2 수학과외` 2회 / `수학과외` 3회 / `풍덕천동` 4회 / `수지` 4회. 이 정도가 상한선이다.

밀도 확인:
```powershell
$h=[IO.File]::ReadAllText("blog\파일명.html",[Text.Encoding]::UTF8)
$b=$h.Substring($h.IndexOf('<article>')); $b=$b.Substring(0,$b.IndexOf('</article>'))
$b=[regex]::Replace([regex]::Replace($b,'<[^>]+>',' '),'\s+',' ')
'중2 수학과외','풍덕천동' | %{ "{0} {1}회" -f $_, ([regex]::Matches($b,$_)).Count }
```

### ⚠️ 지어내지 말 것
학교명은 데이터에 있는 **사실**이지만, **그 학교의 시험 특징은 우리가 모른다.** "OO중은 서술형 비중이 40%" 같은 문장은 실재하는 학교에 대한 허위 정보다. 학교명은 *"이 동네 아이들이 다니는 학교"* 로만 쓰고, 학습 조언은 학년·과목 일반론으로 쓴다. 후기·성적 상승 사례도 지어내지 않는다.

### 하단 링크 (내부 링크 장치)
글 끝 `bl-links` 에서 **정확한 앵커 텍스트**로 링크를 보낸다. 이게 매일 센터 페이지 195개에 신호를 보내는 장치다.
```html
<a href="../c/{센터명}.html">{시군구} {동} {과목}과외 선생님</a>
<a href="../teachers.html?way=방문">{시군구} 방문과외 선생님 찾기</a>
<a href="../teachers.html?way=화상">{학년} {과목} 화상과외 선생님</a>
```

### 티칭코칭과 겹치는 문제
같은 축을 쓰므로 겹칠 수 있다는 점은 사용자에게 전달했고, 그래도 이 축으로 간다는 결정이다. 겹침을 줄이는 장치는 두 가지다.
- 티칭코칭은 `{구} {학교명} {과목}과외`로 **구 단위**, 우리는 센터가 있는 **동 단위**다.
- 우리에겐 "학습센터 등원"이라는 선택지가 하나 더 있다. 세 방식 비교는 우리만 쓸 수 있는 내용이다.

## 아직 안 한 것 / 다음 후보
- `status.html` 수업 현황이 **예시 데이터**다. 실제 수업으로 교체하거나, 구글 시트에 `수업현황` 탭을 만들어 읽어오게 하면 커밋 없이 갱신할 수 있다. (페이지에는 "매주 월요일 업데이트"라고 적혀 있지만 자동 갱신 장치는 없다.)
- 구글 서치콘솔 / 네이버 서치어드바이저에 `sitemap.xml` 재제출 (951 URL로 늘었다) + **`rss.xml` 등록**
  네이버 서치어드바이저 → 요청 → RSS 제출에 `https://firststudy.co.kr/rss.xml` 을 넣어 두면 새 글 수집이 빨라진다.
- 데일리 글쓰기를 **예약 자동화**로 돌릴지는 아직 안 정했다. 지금은 사람이 세션을 열어 쓴다.

## 다른 PC 에서 이어서 하기
1. `git clone https://github.com/x26589334-cpu/tutoring-site.git`
2. 선생님 데이터를 재생성하려면 `wawa-renewal`, `gwaoe-page` 도 같이 clone
3. 그 폴더에서 Claude Code 실행 → "공부의 온도(firststudy) 이어서 작업해줘"

## 사이트 안 검색 — 공용 부품 `search-kit.js`

방문자가 쓰는 검색은 이 부품을 통해 돌아간다. 하는 일은 두 가지다.

1. **띄어쓰기 무시** — "부산동래구"와 "부산 동래구"가 같은 결과. 붙여 친 말은 2글자 이상 조각으로 쪼개 찾는다. 학교 정식명칭↔약칭(중학교↔중)도 통일한다.
2. **연관 검색어** — 결과에 많이 나오는 값을 검색어에 이어 칩으로 제시. 결과 0건이면 걸리는 조각을 찾아 대안을 제시한다.

⚠️ **이 파일은 8개 저장소에 같은 내용으로 복사돼 있다**(와와·퍼펙트에듀·더나은에듀·국제학교·키즈튜터·공부의온도·H포스·픽프리).
고칠 일이 있으면 한 곳만 고치지 말고 전부 갱신할 것. 목록은 `사이트관리/사이트대장.md` 참고.
⚠️ 검색 코드보다 **먼저 로드**해야 한다(인라인 스크립트가 위에 있으면 그보다 위에).

